## The statement import numpy as np is a Python command that imports the NumPy library, a powerful package for numerical computing, and gives it the alias np for convenience. This allows you to use NumPy functions and objects by prefixing them with np, such as np.array() for creating arrays.

import numpy as np 

## matplotlib is a famous library to plot graphs in Python.

import matplotlib.pyplot as plt


